import { GetAddressOptionalMiddleware } from './get-address-optional.middleware';

describe('GetAddressOptionalMiddleware', () => {
  it('should be defined', () => {
    expect(new GetAddressOptionalMiddleware()).toBeDefined();
  });
});
